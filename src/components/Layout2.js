import { Button } from 'react-bootstrap';

const title = "Groovy - Layout 2"


export function Layout2() {
    return (
        <>
            <h1>{title}</h1>
            <Button variant="primary">Something</Button>
        </>
    );
}
